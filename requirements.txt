# Web API
fastapi==0.138.0
uvicorn==0.40.0
python-multipart==0.0.32
python-dotenv==1.2.2
loguru==0.7.3

# Database and migrations
SQLAlchemy==2.0.51
aiomysql==0.3.2
psycopg[binary]==3.3.4
alembic==1.19.1

# Authentication and validation
pydantic==2.13.4
passlib==1.7.4
bcrypt==4.0.1

# LangChain / LangGraph
langchain-core==1.6.0
langchain-community==0.4.1
langchain-text-splitters==1.1.2
langchain-deepseek==1.1.0
langchain-ollama==1.1.0
langchain-redis==0.2.5
langgraph==1.0.7
# langgraph-prebuilt 必须钉住：>=1.0.8 会从 langgraph.runtime 导入 ExecutionInfo，
# 而 langgraph 1.0.7 没有这个符号，全新 pip install 后 `from langgraph.prebuilt import ToolNode`
# 会直接 ImportError，整个后端起不来。
langgraph-prebuilt==1.0.7
langgraph-checkpoint-postgres==3.1.2

# Redis, retrieval and search
redis==7.4.1
redisvl==0.26.0
requests==2.34.2

# AI tools and file parsing
numexpr==2.14.1
pypdf==6.3.0
python-docx==1.2.0
openpyxl==3.1.5
aiofiles==25.1.0
