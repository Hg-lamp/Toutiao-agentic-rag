# 新闻资讯 AI 平台需求文档

## 1. 项目目标

构建一个面向新闻阅读场景的 AI 应用：用户可以浏览新闻、管理个人内容，
并通过具备联网搜索、知识库检索和文件分析能力的 Agent 获取可追问的答案。

## 2. 用户角色

| 角色 | 说明 |
|---|---|
| 游客 | 查看公开新闻和 API 文档 |
| 注册用户 | 登录后使用收藏、历史、AI 对话、文件上传和个人知识库 |
| 系统管理员 | 维护部署环境、数据库和外部服务，不在当前 Web 端提供管理页面 |

## 3. 功能需求

### 3.1 用户与内容

- 支持注册、登录、获取和修改个人资料、修改密码、上传头像。
- 支持按分类分页浏览新闻、查看新闻详情。
- 支持收藏、取消收藏、查看收藏列表和清空收藏。
- 支持记录浏览历史、删除单条历史和清空历史。

### 3.2 AI 对话

- 登录用户可以通过 SSE 流式接口发送问题并接收回答。
- 对话由 LangGraph 编排，支持 ReAct 循环，最大递归深度为 100。
- Agent 可按需使用 SearXNG 联网搜索、Redis 向量检索、计算器和子 Agent。
- 对话必须按用户隔离，不能读取其他用户的会话或知识库。
- 支持分页查看会话列表、查看历史消息和删除会话。
- 通过 MySQL 保存会话元信息和消息列表，通过 PostgreSQL Checkpoint 保存图状态。

### 3.3 文件与知识库

- 支持 TXT、MD、PDF、DOCX、XLSX、CSV。
- 单文件最大 5 MB，解析文本最大 100,000 字，超出部分截断。
- 普通上传只解析文本并注入当前对话，不在服务端持久化原文件。
- 知识库上传需要登录，将文本切分后写入当前用户专属 Redis 向量索引。
- 文件解析或向量库不可用时，接口返回明确的 4xx/5xx 错误。

### 3.4 用户记忆

- 系统可以从用户对话中提取长期偏好，保存到用户记忆表。
- 记忆仅用于该用户后续会话的上下文增强。
- 记忆写入失败不能伪装成成功，应记录错误并返回可诊断信息。

## 4. 非功能需求

- 后端使用 Python 3.12、FastAPI 和异步 SQLAlchemy。
- 前端使用 Vue 3、Vant 和 Vite。
- 所有外部服务地址和密钥通过环境变量配置，禁止提交真实密钥。
- Docker Compose 应能启动后端及 MySQL、Redis、Redis Stack、PostgreSQL、SearXNG。
- 上传文件和数据库数据使用 Docker volume 持久化。
- API 应提供 Swagger（`/docs`）和 ReDoc（`/redoc`）文档。

## 5. 服务与端口

| 服务 | 容器端口 | 宿主机端口 | 用途 |
|---|---:|---:|---|
| backend | 8000 | 8000 | FastAPI API |
| mysql | 3306 | 3306 | 业务数据库 |
| redis-cache | 6379 | 6379 | 缓存 |
| redis-vector | 6379 | 6380 | Redis 向量存储 |
| postgres | 5432 | 5432 | LangGraph Checkpoint |
| searxng | 8080 | 8080 | 联网搜索 |
| Ollama | 11434 | 11434 | 嵌入模型（宿主机运行） |

## 6. 验收标准

1. `docker compose up -d --build` 后，所有基础设施容器通过健康检查。
2. `GET /`、`/docs` 可访问，后端容器健康检查为 `healthy`。
3. 注册并登录用户可以完成新闻浏览、收藏、历史和 AI 对话。
4. AI 对话可以流式返回结果，且会话列表和历史消息可重新加载。
5. 上传支持格式内的文件可以成功解析；超限或不支持格式会得到明确错误。
6. 知识库上传后，后续同一用户的问题能够检索到对应内容。
7. 重启后端容器，数据库、会话和上传数据仍保留。
